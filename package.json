{
  "name": "green-desk",
  "version": "0.1.0",
  "description": "Green Desk",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=packages/api",
    "dev:web": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=packages/api"
  }
}
